import './styles.css';

const App = () => {
    const name = "tesst";
    return (
        <div>React Typescript starter template - {process.env.NODE_ENV} - { name }</div>
    );
};

export default App;
